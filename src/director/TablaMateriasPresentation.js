import React, {Component} from 'react';
import {Table, Column, Cell} from 'fixed-data-table';

const TablaMateriasPresentation = (props) => {
    return (
        <div className=""></div>
    );
};

export default TablaMateriasPresentation;
